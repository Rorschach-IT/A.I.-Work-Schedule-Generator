<Window x:Class="GeneticAlgorithm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm" 
        xmlns:viewmodel="clr-namespace:GeneticAlgorithm.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="auto" Width="820">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Work Schedule" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30"/>
        <ListView ItemsSource="{Binding WorkSchedules}" Margin="5" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding FormattedDate}" Width="150" />
                    <GridViewColumn Header="Day" DisplayMemberBinding="{Binding DayOfWeek}" Width="120" />
                    <GridViewColumn Header="Change ID" DisplayMemberBinding="{Binding ChangeId}" Width="120" />
                    <GridViewColumn Header="Client Counter" DisplayMemberBinding="{Binding ClientCounter}" Width="150" />
                    <GridViewColumn Header="Employees" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding EmployeeId}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="LightGray" Margin="2" Padding="4">
                                                <TextBlock Text="{Binding}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
