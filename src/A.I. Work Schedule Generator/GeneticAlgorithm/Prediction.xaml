<Window x:Class="GeneticAlgorithm.Prediction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm"
        mc:Ignorable="d"
        Title="Prediction"
        Height="auto" Width="auto"
        SizeChanged="Window_SizeChanged">
    <Window.Resources>
        <Style x:Key="CustomGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
               Text="Work Schedule" 
               VerticalAlignment="Center" 
               HorizontalAlignment="Center" 
               FontSize="30"
               Margin="10 20 10 10"/>

        <Button Grid.Column="1" 
            Grid.Row="0" 
            Content="Accept" 
            Margin="10 30 15 10"
            Padding="10"
            Cursor="Hand"
            Width="120"
            HorizontalAlignment="Right"
            Background="LightGreen"/>

        <Button Grid.Column="2" 
            Grid.Row="0" 
            Content="Decline" 
            Margin="10 30 15 10"
            Padding="10"
            Cursor="Hand"
            Width="120"
            HorizontalAlignment="Right"
            Background="Red"/>

        <ListView x:Name="WorkScheduleListView"
              Grid.ColumnSpan="3" 
              ItemsSource="{Binding WorkSchedules}" 
              Margin="15"
              Grid.Row="1" 
              Width="auto">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding FormattedDate}"/>
                    <GridViewColumn Header="Day" DisplayMemberBinding="{Binding DayOfWeek}"/>
                    <GridViewColumn Header="Change ID" DisplayMemberBinding="{Binding ChangeId}"/>
                    <GridViewColumn Header="Employees Counter">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding EmployeesId}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="LightGray" Margin="2" Padding="4">
                                                <TextBlock Text="{Binding}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
